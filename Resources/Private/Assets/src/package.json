{
  "name": "vite-build",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "build": "npm run build-css && npm run build-js",
    "build-css": "vite build --config css.config.js --emptyOutDir ",
    "build-js": "vite build --config js.config.js --emptyOutDir "
  },
  "devDependencies": {
    "@vitejs/plugin-legacy": "^4.1.1",
    "sass": "^1.69.4",
    "terser": "^5.22.0",
    "vite": "^4.5.0"
  }
}
